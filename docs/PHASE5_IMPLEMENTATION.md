# Phase 5: passive gaming notices

Phase 5 only, on owner-requested `phase5-gaming-safe-notifications`, based on `screentime-dev` at `6737bae3a714bd9c971bb6a160b5d47c2eeda90e`. The task explicitly stops with an open PR for independent review; it does not authorize merging. The owner passed final refined-notification/full-countdown acceptance during active Apex firing-range gameplay on `eca318ed00c677980bb86d3b62a6bb33fd0268c0`. See [the acceptance record](PHASE5_APEX_ACCEPTANCE.md). The only subsequent executable change replaces the preview's synthetic label with `Example App`; production `DisplayName` identification and all notification/enforcement behavior remain unchanged. Final independent review of this label/documentation follow-up is pending.

## Implementation and authority

The interactive `WarningPopup` and `BlockedPopup` are removed. `PassiveNoticeWindow` is the single automatic notice surface. It has passive text (including the approved once-per-second final-minute countdown), no buttons, links, close controls, acknowledgement, animation, input registration or game integration. `App` consumes Core's bounded notification mailbox without invoking policy or persistence from WPF. No tray/status/protected-action work is included.

`NotificationPolicy` derives candidates from the current daily quota and committed `PolicyDecision.Grace`. The sequence is approximately ten and five minutes of daily quota remaining, grace start, five minutes of grace, then the approved final-minute countdown. First observation below a threshold may emit the current notice. Skipped milestones coalesce to the most urgent current milestone. Grace recovery formats the original absolute deadline and actual remaining time; it never creates an episode or calculates a replacement deadline. The text states quota exhaustion, temporary continuation for the captured session, denial of new launches, and the fixed local stop time. The optional blocked notice distinguishes downtime from quota and includes projected next availability when known.

The Phase 4 coordinator still commits usage/episode state, evaluates exact instances and enforces before publishing notice candidates. `NotificationOutbox` owns bounded best-effort receipt IO on a separate background task, and `NotificationReceiptStore` uses a **separate SQLite file and writer lock**. A receipt transaction cannot hold the authoritative database's writer lock. The background task alone did not provide this isolation in the original PR head; independent review R1 identified and reproduced that correctness defect. The remediation and real-lock regression are recorded below. The outbox coalesces pending requests by app (at most eight); its delivery channel also holds at most eight. WPF polls only that mailbox every 250 ms and coalesces delivery to one surface. It never enumerates game processes or reads SQLite. A receipt/display failure suppresses that attempt; it cannot grant time or change a capture set. Authoritative persistence failures remain errors and still prevent uncommitted enforcement. Orderly shutdown observes the receipt task.

For prior-phase compatibility, the existing warning flag and legacy Core callback tests remain; the application no longer subscribes to those callbacks. The new notification route neither reads nor uses `WarningSent` as a receipt. Existing quota/grace/downtime accounting and Windows enforcement adapters are unchanged.

## Receipts, stale requests and migration

The authoritative database remains at schema **4**, retaining its original `NotificationReceipts` table solely as a read-only import source. New receipt writes go to `<database>.notifications.db`, a separately versioned **schema 1** database containing `NotificationReceipts(ReceiptKey, AppKey, Kind, RequestedAtUtcTicks)`. Receipt keys are canonical app/local quota date/milestone for quota notices and immutable episode ID/milestone for grace notices. Keys survive rule deletion/recreation. A selected five-minute milestone also suppresses an earlier ten-minute request after a same-day allowance increase; the analogous rule applies to final grace versus grace start. Receipt rows mean **selected for best-effort delivery**, never proof of display or acknowledgement. Runtime attempts prevent ordinary ticks from retrying the same current candidate after receipt failure. Blocked notices use a runtime thirty-second per-app cooldown and do not accumulate durable launch history.

The existing versioned migration transaction and consistent SQLite backup discipline now extend through version 4; existing databases get `<database>.pre-phase5.bak`, fresh databases do not. Unknown newer versions are rejected. A collision rolls back the schema/version and preserves the backup. Prior migration fixtures explicitly remove the new table before emulating older versions. No installed TimeGuard profile is opened or migrated.

The separate receipt file initializes lazily on the outbox worker, so its corruption, unsupported schema or initialization failure does not prevent monitor startup. On first initialization it reads the legacy table through a read-only connection, closes that connection, then atomically creates/copies/version-stamps the destination. It never attaches the policy database to a receipt transaction or deletes source rows. Once schema 1 is committed, later receipt operations do not read the legacy table. A failed import rolls back all destination rows/schema/version and can retry without losing source receipts. An existing unversioned destination receives a consistent `.pre-v1.bak` before attempted migration; unknown newer versions are rejected without recreation. Both profile database files and their SQLite sidecars belong in profile backups. Receipt persistence errors remain best-effort failures contained by the outbox; authoritative database errors are not suppressed.

Receipts are deliberately separate from the authoritative usage/grace commit: a notification-storage failure must not prevent a grant or enforcement. A crash after grace commit but before receipt may lead to a recovery notice from that same episode, with its original deadline. A crash after receipt but before display may lose the notice. This is best-effort external delivery, not an exactly-once display guarantee. Notification code has no API for writing grace.

Ordinary requests expire after fifteen seconds (or earlier at the grace deadline). Final-minute requests expire at that original deadline, require matching active episode/deadline facts and can only display within its last sixty seconds. Core rechecks the current candidate and configuration revision before receipt/display and WPF checks again throughout visibility. Reload invalidates queued notices. Quota exhaustion, a newer milestone, confirmed exit, expiry and sleep past validity discard stale requests. No backlog of old notices is replayed. `NotificationFacts`, `Decisions` and `GraceEpisodes` expose read-only facts suitable for future Phase 6 status.

The countdown is live state, runtime-deduplicated per episode, and deliberately bypasses durable delivery receipts. It may resume after restart for only the original remaining seconds; no new receipt schema/version is necessary and no per-second database writes occur. Core retains ordinary five-second discovery and adds a single known-boundary wake at deadline minus sixty seconds, alongside its existing unchanged deadline wake. A slow/failed outbox may suppress this best-effort presentation but cannot defer enforcement. WPF's one-second timer formats a copied deadline; it never calls SQLite, policy or a terminator. The immutable deadline/capture transaction and expiry-before-enforcement implementation are unchanged.

## Focus and input design

`ShowActivated=false`, `Focusable=false`, disabled WPF hit testing and no keyboard tab navigation are established before showing. Source initialization applies `WS_EX_NOACTIVATE`, `WS_EX_TOOLWINDOW`, `WS_EX_LAYERED`, and `WS_EX_TRANSPARENT` to ScreenTime's own HWND. `WM_MOUSEACTIVATE` returns `MA_NOACTIVATE`; `WM_NCHITTEST` defensively returns `HTTRANSPARENT`. Cross-process pass-through relies on the layered-window/transparent-style combination, rather than WPF hit testing or `HTTRANSPARENT` alone. See Microsoft's [layered-window documentation](https://learn.microsoft.com/en-us/windows/win32/winmsg/window-features#layered-windows) and [extended styles](https://learn.microsoft.com/en-us/windows/win32/winmsg/extended-window-styles).

The small surface is briefly topmost, positioned with `SWP_NOACTIVATE` in the foreground window's monitor work area using native pixel bounds and WPF DPI conversion. It is never owned by, attached to, or used to activate the game. It has no taskbar entry. The implementation does not call Activate/Focus/SetForegroundWindow or register input/controller hooks. Exclusive-fullscreen visibility is not guaranteed, and the implementation does not attempt to force visibility by changing fullscreen state. Mixed-DPI/monitor configurations remain a manual target-hardware check.

A presentation-only lifetime closes ordinary notices after approximately six seconds. The approved countdown uses the earlier of the original deadline, request validity, and its at-show remaining time capped at sixty seconds. Monotonic elapsed time and a UTC upper bound prevent clock rollback from increasing the displayed remaining time or prolonging the surface; overdue/late requests expire rather than restarting a minute. It updates text once per second, while the existing 250 ms validity/lifetime check remains. Closed stops both timers; invalidated notices close sooner. An unresponsive WPF dispatcher can delay visual dismissal until it resumes; enforcement continues independently. This limitation must not be represented as an unconditional native timeout guarantee.

## Owner-approved presentation refinements

Following the owner's ten-notice functional firing-range result, notices now center text and branding, use `SizeToContent.Height` rather than fixed 190 px height, and wrap at a 380 DIP width (280/420 min/max) with 128–360 DIP height bounds and monitor-work-area caps. Long user labels are abbreviated at eighty characters so the policy explanation remains readable. The shared `Branding/ScreenTimeBranding.xaml` exposes the replaceable `ScreenTimeBrandImage` resource: an interim neutral clock mark, not the Phase 8 approved hourglass. Notifications reference its resource key; Phase 8 can replace it without notification-specific code changes and converge later tray/window/taskbar/packaging branding. No Phase 8 mass rename or broader branding rollout is included.

Short uppercase headings communicate urgency independently of color: TIME REMAINING, 5 MINUTES LEFT, FINISH YOUR SESSION, FINAL WARNING, FINAL MINUTE, and TIME EXPIRED (APP BLOCKED during downtime). Normal-case body text stays white on a neutral dark background. Blue informational, amber warning, orange grace-start and pale-red critical accents are confined to the heading/countdown and a thin top border. The logo/ScreenTime branding is centered above the message. Native no-activate, tool-window, layered pass-through, message responses and `SWP_NOACTIVATE` remain intact; native positioning now measures content and reapplies placement after size changes.

The owner also approved Phase 6 notification presets/preferences in [D21](DECISIONS.md) and the product contract. No settings controls, preference schema or configurable policy are implemented in this PR.

`--notice-diagnostics` opts into local bounded `notice-diagnostics.jsonl` (1 MiB plus one rotated file). Samples contain timestamp, notice HWND, foreground before/after, the WPF thread's active/focus HWNDs, WPF keyboard-focus/mouse-capture booleans and extended styles. There are no window titles, keyboard contents or game content. Production defaults do not sample/write these diagnostics. Samples are observations at show and 250 ms intervals, not proof that no sub-sample transition ever occurred.

## Tests and current evidence

Final preview-label follow-up after owner acceptance on `eca318e`: the sole executable edit is `StartNoticePreview`'s synthetic `DisplayName` (`ScreenTime preview` → `Example App`). No production body formatter, styles, native handling, timers, Core, storage, enforcement or tests changed. Focused validation:

- `dotnet build TimeGuard.sln --no-restore --no-incremental -c Release`: passed, zero errors, baseline DashboardWindow CS0618 only.
- `dotnet test tests/TimeGuard.UITests/TimeGuard.UITests.csproj --no-build -c Release --filter 'FullyQualifiedName~NoticePresentationTests|FullyQualifiedName~NotificationWindowTests' --logger trx --results-directory tests/TimeGuard.Tests/bin/Phase5PreviewLabel/UI`: **10 passed, zero failed/skipped**, 1m36s on the interactive desktop. Existing application-name body assertions and six-state native input/countdown checks remain unchanged. Inspected the captured preview: Example App appears beneath ScreenTime branding.
- All three installed TimeGuard file paths/hashes and seven HKCU Run values were unchanged in read-only before/after checks. Documentation file links, preview-script parser syntax and whitespace checks passed.
- The full 193 Core / 45 UI-integration results below belong to the previously reviewed `eca318e`; this label-only follow-up reran focused local tests, not the full local suites or Apex gameplay. No new game/enforcement run is claimed. Final independent review of the minimal delta and owner acceptance evidence remains pending; no merge is authorized.

`NotificationTests` covers the complete milestone sequence, repeated polling, restart receipts, fixed episode/deadline, late recovery with actual remaining grace, skipped/stale requests, configuration invalidation, receipt failures, a deliberately stalled receipt writer, continued relaunch/downtime denial and hard enforcement at the original deadline. Schema tests cover version-three backup, idempotence, collision rollback and grace preservation. Prior Phase 1–4 Core assertions remain, with only latest-schema fixture/version adjustments.

`PopupTests` retains real owned-helper denial/relaunch assertions while replacing the intentionally removed OK-button behavior with no-button and automatic-dismissal assertions. `NotificationWindowTests` now displays all six notice kinds through the production renderer against a maximized, separately running fixture-owned helper. It retains native styles/messages, foreground/active/focus samples, lack of buttons, real mouse/keyboard delivery through the opaque notice, non-minimization and five-to-eight-second ordinary dismissal assertions. It adds dynamic-height/heading/logo assertions, screenshots, a full final-minute countdown with decreasing once-per-second text, and deadline dismissal. The helper writes only input counts. Preview mode is asserted to create no database.

Presentation-refinement validation on `eca318e`, 2026-09-24, Windows/.NET 8, Release (historical evidence before the label-only follow-up; local artifacts: `tests/TimeGuard.Tests/bin/Phase5Presentation/`):

- `dotnet restore TimeGuard.sln`: passed with existing configuration.
- `dotnet build TimeGuard.sln --no-restore --no-incremental -c Release`: passed, zero errors, only baseline DashboardWindow CS0618. The prior preview PowerShell session held a Core DLL lock; the owner closed it. No process was killed to unblock the build.
- `dotnet test tests/TimeGuard.Tests/TimeGuard.Tests.csproj --no-build -c Release --logger trx --results-directory tests/TimeGuard.Tests/bin/Phase5Presentation/Core`: **193 passed, zero failed/skipped**. Three added cases verify the 61-to-60-second boundary, matching original persisted deadline, no countdown receipt writes across repeated ticks, restart with only forty seconds remaining, expiry enforcement without UI, and exit/config invalidation. All real-SQLite contention/error-ordering regressions remain passing.
- `dotnet test tests/TimeGuard.UITests/TimeGuard.UITests.csproj --no-build -c Release --logger trx --results-directory tests/TimeGuard.Tests/bin/Phase5Presentation/UI`: **45 passed, zero failed/skipped**, 4m02s, serialized on the interactive desktop. All previous 36 cases remain, including Phase 3/4 real owned-helper integration. Nine added presentation cases cover late delivery, monotonic/UTC rollback/forward/overdue lifetime, missing deadline, sixty-second cap, centered wrapping/natural height, shared-resource replacement and text-based urgency.
- The native six-state probe recorded **370 samples**, preserved helper foreground, no notice activation/focus/capture/minimization, and six mouse/key pairs received by the separate helper. Styles and native message assertions passed. Ordinary visible durations: **6.045, 6.054, 6.096, 6.056, 6.028 seconds**. Final countdown: **60.027 seconds** observed show-to-close, with sixty descending once-per-second values. The 27 ms excess is observed dispatcher/sampling timing, not an extended grace deadline; no exact hard real-time visual timeout is claimed. Enforcement remains independent of visual timer latency.
- Actual notice sizes at this setup were **380 × 136.18 DIPs** for short notices and **380 × 178.74 DIPs** for grace/countdown. Inspected captured quota, grace and countdown screenshots: centered logo/text, restrained accents, readable wrapping and no clipped explanation. The long-label layout/resource replacement regression also passed. Mixed-DPI/scaling/fullscreen and owner visual acceptance of the new presentation remain manual.
- Initial focused layout verification attempted to measure a hidden WPF Window and returned zero size. It now measures the content, as native content-driven placement does; the full suite passes. UI foreground acquisition and input assertions were retained, not relaxed.
- Read-only before/after preservation: **three installed TimeGuard file paths/hashes and seven HKCU Run values unchanged**. Preview script parser syntax and whitespace checks passed. Screenshots/TRX/native samples remain in ignored local build output.

No destructive Apex enforcement, new actual twenty-minute helper run, physical sleep or real-calendar transition was performed in those automated runs. The stored grace duration/accounting/deadline algorithm was unchanged; deterministic original-deadline/error tests, short real-helper scenarios and a real sixty-second display run were exercised. The earlier ten-notice owner observation did not certify the refined visuals/countdown; the subsequent independent review and final owner acceptance are recorded below. Do not merge.

Initial implementation validation on 2026-09-24, head `daecf05`, Windows/.NET 8, isolated profiles (historical; current remediation evidence follows):

- `dotnet restore TimeGuard.sln`: passed using the existing NuGet configuration; initial sandbox access required an approved retry. No credential/configuration changes.
- `dotnet build TimeGuard.sln --no-restore --no-incremental -c Release`: passed, zero errors; only the baseline `DashboardWindow.xaml.cs` CS0618 warning.
- `dotnet test tests/TimeGuard.Tests/TimeGuard.Tests.csproj --no-build -c Release --logger trx`: **183 passed, zero failed/skipped** (174 prior Core cases plus nine Phase 5 cases). Final artifact directory: `tests/TimeGuard.Tests/bin/Phase5Validation/FinalCore`.
- Focused real-helper/passive-block regression: **7 passed**, covering Phase 3, Phase 4 and updated PopupTests. These use only fixture-owned `ScreenTime.TestProcess` identities.
- Corrected native/input probe: **1 passed**, all five notice kinds. **130 samples**, helper foreground unchanged; no notice active/focused, no keyboard focus/mouse capture, native style/message checks pass, five clicks and five key presses received by the separate helper, no minimization. Measured visible durations: **5.993, 6.027, 6.025, 6.007, 6.007 seconds**. This setup reported 420×190 notice bounds; this is not mixed-DPI/exclusive-fullscreen or controller evidence.
- The initial full UI run had **35 passed / 1 failed** in the new input probe. Investigation found the probe's counter-file reader denied concurrent writes by its own helper, allowing a helper IO-error dialog to invalidate foreground/input observations. The test reader now permits write-sharing and tolerates partial reads. Earlier failure records remain local; production activation behavior was not changed to hide this test failure. The focused corrected probe passed all input/focus assertions.
- Final `dotnet test tests/TimeGuard.UITests/TimeGuard.UITests.csproj --no-build -c Release --logger trx`: **36 passed, zero failed/skipped**, including all prior UI/integration scenarios and the new five-kind native/input probe. Duration 3m18s. Final artifact directory: `tests/TimeGuard.Tests/bin/Phase5Validation/FinalUI`. All desktop runs were serialized.
- `git diff --check` and `git diff --cached --check`: passed. Manual preview PowerShell example: parser syntax check passed.
- Read-only preservation comparison: **three installed TimeGuard file paths/hashes unchanged; all seven HKCU Run names/values unchanged**. No installed profile, registration or live application was used as a destructive fixture.

For those initial implementation runs, local TRX/diagnostic artifacts stayed under ignored build output and were not included in the PR. Short real-process tests and deterministic original-deadline checks were rerun; the real twenty-minute helper gate from Phase 4 was not rerun because the grace duration, accounting and deadline algorithm were unchanged. Its historical result was not presented as a Phase 5 run. Those automated runs supplied no physical sleep/hibernate, real-calendar midnight, controller hardware, Apex, frame-time/performance budget or mixed-display-mode acceptance. The later owner observation is recorded separately.

## Independent-review remediation

Retrieved the complete [review](https://github.com/dev-kaj223/ScreenTime/pull/1#pullrequestreview-5310173976), all inline comments and paginated thread resolution state directly with `gh`. Reviewed base was `6737bae3a714bd9c971bb6a160b5d47c2eeda90e`, original head `daecf0576bea60f48233dc21e0331fe1387c3289`. There were two blocking findings and no non-blocking findings.

- **[R1](https://github.com/dev-kaj223/ScreenTime/pull/1#discussion_r4098432351): production correction implemented, awaiting independent re-review.** The original outbox wrote receipts into the policy database, so a slow receipt INSERT could block `CommitObservation` and hence enforcement. Moving the work to another thread was insufficient; the earlier delegate-stall test did not exercise SQLite locking. The separate receipt file fixes that shared-writer-lock path without changing `CommitObservation`, the coordinator's ordering, grace duration/deadline/captures, or error propagation.
- **Real-SQLite regression:** `NotificationContentionTests.RealReceiptInsert_HoldsWriterLock_ExpiryCommitsBeforeEnforcement_OrFailsClosed` executes the production receipt INSERT with a test-only connection-local SQL function invoked from a `BEFORE INSERT` trigger. A competing connection to the receipt file must receive `SQLITE_BUSY`, proving a real writer transaction is held. While it stays held, the expiry tick must finish within three seconds and the terminator reads the already-committed Expired phase and original deadline from SQLite. Both successful and subsequently failing receipt transactions are covered. A third case injects an authoritative expiry-commit error and requires that error to reach the caller with zero termination calls and the stored episode still Active. No process is terminated by these Core tests.
- **Migration regressions:** legacy schema-four receipts import once and still deduplicate after reopening; normal operations after import no longer require the legacy table; collision/unknown-version failures preserve data/version; a bad second imported row rolls back the first row, table and version before an idempotent retry. The existing receipt-failure test now injects failure into the actual separate receipt database.
- **[R2](https://github.com/dev-kaj223/ScreenTime/pull/1#discussion_r4098432364): at the receipt-remediation stage, automated rerun and manual preparation were complete; human acceptance was OPEN.** The reviewer's failed helper-foreground precondition occurred before any notice was requested. Production activation/focus behavior and the UI test were unchanged by that remediation. The acceptance record then said NOT RUN; it now records the owner's later successful firing-range observations, including final acceptance on `eca318e`. This PR must remain unmerged pending the requested final review.

Remediation validation on 2026-09-24 (local artifacts: `tests/TimeGuard.Tests/bin/Phase5Remediation/`):

- `dotnet restore TimeGuard.sln`: passed with the existing NuGet configuration.
- `dotnet build TimeGuard.sln --no-restore --no-incremental -c Release`: passed, zero errors, baseline DashboardWindow CS0618 only.
- `dotnet test tests/TimeGuard.Tests/TimeGuard.Tests.csproj --no-build -c Release --logger trx --results-directory tests/TimeGuard.Tests/bin/Phase5Remediation/FinalCore`: **190 passed, zero failed/skipped**. Real-lock expiry runs completed in **18.3 ms** (receipt eventually succeeds) and **31.0 ms** (receipt subsequently fails) while the receipt transaction remained blocked. The authoritative-commit negative case propagated its error in **2.5 ms**, with no termination. These are isolated regression observations, not a production latency or performance guarantee.
- The first remediation Core run exposed a fixture-seeding error: the seeded grace episode omitted its exhausted quota row, allowing an unrelated fresh-quota notice and invalidating a receipt-count assertion. The fixture now seeds the actual exhausted quota; no production permission or notification assertion was relaxed. Seven new cases cover real contention and migration; all prior 183 cases remain.
- `dotnet test tests/TimeGuard.UITests/TimeGuard.UITests.csproj --no-build -c Release --logger trx --results-directory tests/TimeGuard.Tests/bin/Phase5Remediation/FinalUI`: **36 passed, zero failed/skipped**, in 3m22s on the interactive desktop, including Phase 3/4 real owned-helper integration and the unchanged five-kind native/input test. The final probe recorded **130 samples** with unchanged helper foreground, five received mouse/key pairs, no notice activation/focus/capture or minimization, and visible durations **6.036, 6.061, 6.029, 6.130, 6.055 seconds**. Native style/message assertions passed. An earlier complete remediation UI run also passed all 36 tests; the results here are from the final executable source.
- Read-only preservation comparison: **three installed TimeGuard file paths/hashes and all seven HKCU Run names/values unchanged**. No installed profile, registration or live application was used as a destructive fixture.
- `git diff --check` and `git diff --cached --check`: passed. The manual preview PowerShell example passed its parser syntax check. TRX and diagnostic artifacts remain local under ignored build output.

For that receipt remediation, no production WPF/native activation code or UI-test precondition was changed. The actual twenty-minute duration was not rerun: that correction isolates receipt storage while leaving the authoritative grace/accounting/deadline implementation unchanged; short real-helper and deterministic deadline/error-ordering gates were rerun. The later owner functional observation and presentation changes are recorded separately above and in the acceptance record.

## Owner's manual Apex acceptance procedure

The [independent re-review of `67237e3`](https://github.com/dev-kaj223/ScreenTime/pull/1#pullrequestreview-5310527969) resolved R1. The subsequent [independent review of `eca318e`](https://github.com/dev-kaj223/ScreenTime/pull/1#pullrequestreview-5312071076) found no blocking code issues in the refined presentation/countdown, leaving only final owner acceptance open. The owner has now passed that firing-range acceptance on the same head, as detailed in [the acceptance record](PHASE5_APEX_ACCEPTANCE.md). The minimal preview-label/documentation follow-up still requires final independent review; none of these steps authorizes merging.

**Prerequisite:** clean helper/input validation on an unlocked interactive desktop. No automated WPF result establishes Apex safety. Preserve the installed TimeGuard profile/process/startup state. If another enforcer is already targeting Apex, coordinate that separately; this task does not authorize stopping it. This test must never exercise destructive enforcement against Apex.

1. Use Apex **firing range/training only**. Record Windows build, GPU/driver, display resolution/refresh, scaling and monitor layout, game mode (borderless/exclusive where supported), controller/input device and built-in performance display configuration. Record idle and active gameplay baseline with game-provided performance information; do not add injected capture/graphics tools.
2. Launch the notice-only preview below with a fresh test profile. It does **not** construct a database, monitor, process observer or terminator. It displays synthetic ten-minute, five-minute, grace-start, final-grace, blocked and final-minute countdown text for **Example App** through the same production WPF surface. Production notices instead identify the affected application's `DisplayName`. The first five notices dismiss after about six seconds; the sixth counts down for at most sixty seconds. The displayed times are preview examples, never actual Apex deadlines.
3. Return to the firing range before the delay elapses. For the first and subsequent notices, verify no focus loss, Alt-Tab/minimize/fullscreen transition, held-key interruption, mouse interception, controller interruption, or obvious/reproducible frametime spike. Exercise mouse input beneath several opaque/text locations of the notice. Record visible duration, before/during/after foreground HWND samples and the game's actual fullscreen/minimized behavior.
4. Repeat the sequence for supported display modes/layouts and under representative training load. Compare first-show and later-show frame behavior to baseline. Record actual measurements/observations, including suppression or invisibility, instead of claiming zero stutter. Preserve the local HWND log with the acceptance notes.
5. If any input/focus/rendering regression occurs, stop acceptance, retain reproduction evidence and report it. Do not add native/game hooks or adopt another mechanism without the agreed WPF fallback decision. A failed locked-desktop/input-automation run is an environmental failure, not grounds to modify production focus behavior.
6. Only after training passes should normal **non-ranked** gameplay be considered. Ranked remains a later explicit acceptance gate. No destructive Apex quota/grace expiry test is authorized by this procedure.

PowerShell 7 example, run from the repository after a Release build. The launch is background-only; the actual notice HWNDs become briefly visible. The fifteen-second delay lets the owner return to Apex. This script signals only its unique preview profile and shuts that preview down; it never terminates processes by name.

```powershell
$buildDirectory = Join-Path (Get-Location) 'src\TimeGuard.App\bin\Release\net8.0-windows'
Add-Type -Path (Join-Path $buildDirectory 'TimeGuard.Core.dll')
$previewRoot = Join-Path $env:TEMP ('ScreenTime-preview-' + [Guid]::NewGuid().ToString('N'))
$previewRuntime = [TimeGuard.Services.RuntimeOptions]::Test($previewRoot)
$previewProcess = Start-Process -FilePath (Join-Path $buildDirectory 'TimeGuard.exe') `
    -ArgumentList @('--test-profile', ('"' + $previewRoot + '"'), '--preview-notices') `
    -WindowStyle Hidden -PassThru
Start-Sleep -Seconds 3
$noticeSignal = [Threading.EventWaitHandle]::OpenExisting($previewRuntime.NoticeEventName)
$stopSignal = [Threading.EventWaitHandle]::OpenExisting($previewRuntime.StopEventName)
try {
    Start-Sleep -Seconds 15
    1..6 | ForEach-Object {
        [void]$noticeSignal.Set()
        Start-Sleep -Seconds $(if ($_ -eq 6) { 70 } else { 20 })
    }
} finally {
    [void]$stopSignal.Set()
    $noticeSignal.Dispose()
    $stopSignal.Dispose()
    $previewProcess.WaitForExit(5000)
    $previewProcess.Dispose()
}
Write-Host "Local diagnostic evidence: $previewRoot\notice-diagnostics.jsonl"
```

Close the preview PowerShell session when done; `Add-Type` keeps the loaded Core DLL locked until that session exits, which otherwise blocks a later rebuild. Acceptance record: tested commit/build; UTC/local time; environment listed above; first/subsequent notice kind; foreground before/during/after; minimization/fullscreen state; mouse/keyboard/controller results; visible duration; baseline/notice frametime observations; pass/fail and reproduction notes. The owner result on `eca318e` is recorded separately from automated evidence; final independent review of the label follow-up remains pending. No merge is authorized.
