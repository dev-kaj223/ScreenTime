<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <OutputType>WinExe</OutputType>
    <TargetFramework>net8.0-windows</TargetFramework>
    <Nullable>enable</Nullable>
    <RuntimeIdentifiers>win-x64</RuntimeIdentifiers>
    <RestorePackagesWithLockFile>true</RestorePackagesWithLockFile>
    <ImplicitUsings>enable</ImplicitUsings>
    <UseWPF>true</UseWPF>
    <UseWindowsForms>true</UseWindowsForms>
    <RootNamespace>TimeGuard</RootNamespace>
    <AssemblyName>ScreenTime</AssemblyName>
    <Product>ScreenTime</Product>
    <Version>0.1.0-beta.2</Version>
    <Title>ScreenTime</Title>
    <Description>Local selected-app screen time for Windows.</Description>
    <ApplicationIcon>Branding\ScreenTime.ico</ApplicationIcon>
  </PropertyGroup>

  <PropertyGroup Condition="'$(ScreenTimeDistribution)' == 'true'">
    <DefineConstants>$(DefineConstants);SCREENTIME_DISTRIBUTION</DefineConstants>
    <RuntimeFrameworkVersion>8.0.31</RuntimeFrameworkVersion>
  </PropertyGroup>

  <!-- App.xaml lives in UI/ subfolder; override its build action explicitly -->
  <ItemGroup>
    <Resource Include="Branding\ScreenTime.ico" />
    <Page Remove="UI\App.xaml" />
    <ApplicationDefinition Include="UI\App.xaml">
      <DependentUpon>App.xaml.cs</DependentUpon>
    </ApplicationDefinition>
  </ItemGroup>

  <ItemGroup>
    <ProjectReference Include="..\TimeGuard.Core\TimeGuard.Core.csproj" />
  </ItemGroup>

  <ItemGroup>
    <PackageReference Include="OxyPlot.Wpf" Version="2.1.2" />
  </ItemGroup>

</Project>
